// Cargo es el gestor de paquetes de y a su vez el sistema de construcción de Rust.
// La mayoría de los proyectos en Rust usan Cargo porque facilita muchas tareas. En este caso
// se recreará el código del capítulo 1.1 usando Cargo (cargo run/build/check son explicados
// en este capítulo y el flag --release para compilaciones finales).
fn main() {
  println!("Hello, Cargo!");
}
